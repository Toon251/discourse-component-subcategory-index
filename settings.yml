plugin_outlet:
  default: "discovery-above"
hide_on_mobile:
  default: false
categoryIds: {
    type: list,
    description: "List of category IDs",
    default: ""
}
page_navigate: true
page_size: {
  type: number,
  description: "Category per page",
  default: 10
}